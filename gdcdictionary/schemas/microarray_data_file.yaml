$schema: "http://json-schema.org/draft-04/schema#"

id: "microarray_data_file"
title: Data from microarray study
type: object
namespace: http://ntp.niehs.nih.gov/cebs/
category: microarray_data_file
program: '*'
project: '*'
description: >
  Data from microarray study
additionalProperties: true
submittable: true
validators: null

systemProperties:
  - id
  - project_id
  - created_datetime
  - updated_datetime
  - state
  - file_state
  - error_type

links:
  - exclusive: false
    required: true
    subgroup:
      - name: subjects
        backref: microarray_data_files
        label: data_from
        target_type: subject
        multiplicity: many_to_one
        required: false
      - name: core_metadata_collections
        backref: microarray_data_files
        label: data_from
        target_type: core_metadata_collection
        multiplicity: many_to_many
        required: false

required:
  - subject_id
  - tissue
  - test_article
  - data_format

uniqueKeys:
  - [ id ]
  - [ project_id, submitter_id ]

properties:
  $ref: "_definitions.yaml#/data_file_properties"

  data_category:
    term:
      $ref: "_terms.yaml#/data_category"
    enum:
      - Microarray Data File

  subject_id:
    description: animal id
    type: string

  geo_doi:
    type: string

  data_file_name:
    description: data file GEO accession
    type: string

  test_article:
    description: test article
    type: string

  tissue:
   description: tissue type
   type: string

  assay_type:
    type: string

  dose:
    type: string

  data_format:
    term:
      $ref: "_terms.yaml#/data_format"
    type: string

  data_type:
    description: >
      Specific content type of the data file.
    type: string

  subjects:
    $ref: "_definitions.yaml#/to_one"

  core_metadata_collections:
    $ref: "_definitions.yaml#/to_one"
